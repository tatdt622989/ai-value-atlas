# 本輪驗證 · 2026-09-11

## 實測範圍

- `pnpm check` 通過，涵蓋 Vue、server、shared、scripts 與 tests。
- `ATLAS_INTEGRATION=true pnpm test`：33 項通過，包含真實本機 MongoDB；測試使用隔離的 atlas_test_* DB，結束清理。
- `pnpm build` 通過。正式 build 由 Hono 在 localhost:4318 提供，不是 dev server。
- 背景 Codex IAB 操作；未操作使用者 Chrome 分頁。完成桌面、390×844 手機設定及更寬手機檢查。
- IAB viewport 設定 1536×1024 時 evaluate 回報 1280×853；另測回報 1536×1024 的尺寸。這是工具縮放差異，未將輸出 PNG 像素尺寸宣稱為 CSS 尺寸。原生桌面截圖保存在 `desktop-review.png`，手機在 `mobile-review.png`。手機覆寫截圖工具會保留部分外框空白；DOM 溢出檢查獨立確認。
- 已用 `view_image` 同次檢視設計稿與實際桌面、手機截圖。

## 設計比對紀錄

設計來源：`design-options/value-first.png`；最終依使用者指示納入研究估算與 6yuwei 3D logo。

| 檢查項目 | 設計稿 | 實際結果／修正 |
|---|---|---|
| 主次層級 | 每美元倍率主欄，預算藏在篩選 | 倍率先排序；預算、通路、能力與速度為第二層；預算 $20 與 $10 已測 |
| 配置 | 全寬列表，無側欄／右側面板 | 已移除舊 sidebar 與 inspector，列高約 72 CSS px；詳情為 native dialog |
| 標題與字級 | 小標題、沒有大口號 | h1 17px；桌面倍率 34／32px，手機 29px；修正榜單完整 variant 造成的擁擠 |
| 配色與容器 | 灰白、森林綠、淡萊姆；細分隔線 | 延續同色系與列表容器，沒有額外卡片牆、漸層 hero 或裝飾大面板 |
| Logo | 原圖僅字標，後續要求自己的 3D logo | 使用 imagegen 生成的 6yuwei 原標誌 3D 版本；1254px PNG 載入成功，CSS 縮放，不程式重畫 |
| 操作入口 | 列尾單一箭頭 | 只有 →，沒有「詳情」字樣與框線；有完整 aria-label |
| 手機 | 原稿無獨立手機版 | 窄螢幕保留倍率、方案、模型與月費；次要欄移入彈窗；修正導覽列 2px 溢出，最窄實測 scrollWidth 312 ≤ viewport 325 |
| 原始資料 | 舊示意圖排除部分訂閱 | 使用者明確修正後恢復 164 筆研究、8 個訂閱估算，依原採納欄位進入主榜；這是授權差異，不為貼合示意圖刪資料 |

首屏文案比對：保留「價值排行／模型能力／資料狀態」「比較用途」「比較情境」「篩選」「每 $1 的價值」與 API 1× 基準。預設改為「全部」以適合所有使用者。日期來自資料，新增估算 * 與已查到的日期差異，取代被使用者否決的「無法核實」分類。沒有新增口號或大標題。新增來源條件、價格與列數都是資料結果，並非裝飾。

已依選定視覺方向完成比對；保留的差異是使用者要求的 logo、研究參與排行、實際資料列數，以及必要的手機重排。不宣稱像素級完全相同。

## 操作證據

1. 主榜保留 Claude Pro 59.1471×、ChatGPT Plus 23.5079× 等原研究值；詳細來源／權重與範圍可開啟。
2. 預算 $20 排除較高月費；回歸測試另驗證 $10 能找到實際前期付款 $10 的 OpenCode Go。
3. 使用一半讓 Claude Pro 顯示 29.6×；快取選項不改造未知 token mix 的觀測估算。
4. 搜尋不存在的方案顯示空結果，清除篩選恢復主榜；URL 保留偏好。
5. 手機箭頭彈窗、來源頁籤、加入比較、兩方案比較皆操作成功。
6. 年繳選項顯示月均 $16.67 與年繳一次 $200，不混淆付款週期。
7. 模型頁 WebDev 使用目前映射榜單；Coding 缺 API 資料時顯示接入狀態，不冒充已取得分數。
8. 資料狀態頁顯示真實部分完成紀錄、研究來源差異與 AI key 狀態。瀏覽器最終 error/warn log 為空。

## 更新循環與限制

已實跑一次來源更新：81 成功／7 失敗，2 個完整 catalog 暫存待審，75 個研究來源待核對（含原文基線尚未建立），未覆蓋原值。`.env` 已啟用每日台北 09:00 蒐集；AUTO_PUBLISH=false，服務運行時才會排程。當天已手動更新不重複抓取。

AI 與 Artificial Analysis 金鑰未設定，完整付費 AI 搜尋／獨立審核及 AA live call 尚未實測；不能用 adapter 或單元測試替代這項證據。研究來源仍有逐項核對工作，詳見 VERIFICATION.md。部署依使用者指示暫停。
